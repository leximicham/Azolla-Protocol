{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "run_record",
  "description": "Audit record for one workorder execution.",
  "type": "object",
  "required": [
    "run_id",
    "work_order_id",
    "objective_id",
    "output_id",
    "gate_result",
    "gate_reason",
    "created_at"
  ],
  "properties": {
    "run_id": {"type": "string"},
    "work_order_id": {"type": "string"},
    "objective_id": {"type": "string"},
    "output_id": {"type": "string"},
    "gate_result": {"type": "string", "enum": ["PASS", "FAIL"]},
    "gate_reason": {"type": "string"},
    "commit_sha": {"type": ["string", "null"], "description": "Extracted from output_bundle.metadata for diazotrophs that produce repository state (e.g., PATCH_DIAZOTROPH). Null for other diazotroph types."},
    "created_at": {"type": "string", "format": "date-time"}
  },
  "additionalProperties": false
}
