{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "capture_buffer",
  "description": "A raw external input captured for processing. Universal intake artifact available to any azolla type.",
  "type": "object",
  "required": [
    "capture_id",
    "azolla_id",
    "raw_content",
    "source_label",
    "status",
    "created_at",
    "updated_at"
  ],
  "properties": {
    "capture_id": {"type": "string"},
    "azolla_id": {"type": "string"},
    "raw_content": {"type": "string"},
    "source_label": {
      "type": "string",
      "description": "Operator-assigned label for the capture source (e.g., session-notes, jira-comment, feature-request, voice-memo-transcript)."
    },
    "status": {
      "type": "string",
      "enum": ["PENDING", "PROCESSED"]
    },
    "created_at": {"type": "string", "format": "date-time"},
    "updated_at": {"type": "string", "format": "date-time"}
  },
  "additionalProperties": false
}
